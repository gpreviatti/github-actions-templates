name: Dotnet Setup
description: "Checkout code and setup .NET environment"

inputs:
  dotnet_version:
    required: true
    type: string
    default: '8.x'

runs:
  using: "composite"
  steps:
    - uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ inputs.dotnet_version }}